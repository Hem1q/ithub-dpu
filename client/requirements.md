## Packages
framer-motion | Complex page transitions and gesture animations
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes safely

## Notes
The app relies on the shared schema and routes defined in @shared/routes.
Images for Oracle, Amazon, and Cisco will be sourced from Unsplash.
Authentication is handled via Replit Auth, but we fetch user data from /api/user as per the route manifest.
